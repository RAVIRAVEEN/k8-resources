apiVersion: v1
kind: pod 
metadata:
 name: labels-demo
 labels:
  environment: dev
  app: nginx 
spec:
  containers: 
    - name: hello-pod
      image: nginx
      ports:
         - containerPort: 80 