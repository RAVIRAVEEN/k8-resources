apiVersion: v1
kind: Pod 
metadata:
 name: multi-containers
spec:
 containers:
   - name: nginx
     image: nginx 
     ports:
     - containerPort: 80
containers:
   - name: almalinux
     image: almalinux:8
     command: [ "sleep", "300"]

     